version: '3.9'
services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: cloaklink
      POSTGRES_PASSWORD: cloaklink
      POSTGRES_DB: cloaklink
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "cloaklink"]
      interval: 10s
      timeout: 5s
      retries: 5
  api:
    build:
      context: .
      target: runner
    command: sh -c "node scripts/wait-for-db.js && node api/dist/server.js"
    env_file:
      - api/.env.example
    environment:
      DATABASE_URL: postgresql://cloaklink:cloaklink@postgres:5432/cloaklink?schema=public
      NODE_ENV: production
    ports:
      - '4000:4000'
    depends_on:
      postgres:
        condition: service_healthy
  frontend:
    build:
      context: .
      target: runner
    command: npm --workspace frontend run start
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:4000
    ports:
      - '3000:3000'
    depends_on:
      - api
  indexer:
    build:
      context: .
      target: runner
    command: sh -c "node scripts/wait-for-db.js && node indexer/dist/index.js"
    env_file:
      - indexer/.env.example
    depends_on:
      - api
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://cloaklink:cloaklink@postgres:5432/cloaklink?schema=public
      NODE_ENV: production
    ports:
      - '5001:5001'
volumes:
  postgres-data:
